services:
  tracktime-api:
    build: .
    user: "${UID}:${GID}"
    container_name: tracktime-api
    ports:
      - "80:80"
    volumes:
      - ./public:/var/www/html
      - ./database:/var/www/database
    restart: unless-stopped
